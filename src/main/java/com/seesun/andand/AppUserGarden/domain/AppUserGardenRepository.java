package com.seesun.andand.AppUserGarden.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AppUserGardenRepository extends JpaRepository<AppUserGarden, Long> {
}
